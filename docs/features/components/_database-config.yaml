# Database configuration template
# Reusable database settings for different services

database:
  connection_pool:
    min_size: 5
    max_size: 20
    idle_timeout: 300
  retry_policy:
    max_attempts: 3
    backoff_ms: 100
  ssl:
    enabled: true
    verify: true
