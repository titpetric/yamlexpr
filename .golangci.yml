version: "2"

issues:
  max-issues-per-linter: 0

run:
  issues-exit-code: 0

linters:
  enable:
    - unused
    - govet
    - errcheck
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintln
        - fmt.Fprintf