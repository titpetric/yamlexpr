---
title: "The evaluated if statement is removed from the final result."
---
config:
  name: "production"
  debug:
    if: true
    enabled: true
    level: "verbose"
  version: "1.0"
---
config:
  name: "production"
  debug:
    enabled: true
    level: "verbose"
  version: "1.0"
